import React from 'react';
import PaymentI from '../../../components/PaymentI';

const Page = () => {
    return (
        <div className="min-h-screen bg-gray-100 flex items-center justify-center">
            <PaymentI />
        </div>
    );
};

export default Page;
